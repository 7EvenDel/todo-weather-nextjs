"use client";

const Main = () => {
  return (
    <section className="flex flex-col items-center p-24 bg-cover">
      Content that signed in user can see
    </section>
  );
};

export default Main;
